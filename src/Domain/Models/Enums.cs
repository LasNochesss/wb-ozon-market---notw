namespace Domain.Models;

public enum Marketplace { WB, Ozon, YM, Other }
public enum SaleStatus { Sold, Returned, Cancelled }
public enum SaleType { Real, SelfBuy }
public enum ProductStatus { Active, Stop }
public enum CostType { Ads, Packaging, Warehouse, PhotoContent, Other }
